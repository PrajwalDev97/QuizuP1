//----------------------------------------------------------------------------------------------------------------------
// Log messages
//----------------------------------------------------------------------------------------------------------------------

module.exports.log = (_socket, roomId, message) => {
  const date = new Date().toISOString().replace(/\.\d+/, "").replace("T", " ");
  console.log(`${date} | ${roomId ?? "      "} | ${message}`);
};
